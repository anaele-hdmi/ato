# AGENTS.md — 跡 (ato)

実装担当エージェント向けの要約。原文の指示が最優先。迷ったら「小さく動くもの」を出す。

## 作品
- 画面表記は「跡」のみ。英題なし。起動タイトルなし。
- 定点の小さな家がある丘を、20世紀初頭の春から地質時代まで、スマホの指で見て回る装置。
- 『ちいさいおうち』の構造を借りるが家は救出しない。痕跡が残り、それも消える。ナレーションしない。
- 骨格はコール『帝国の推移』（牧歌→繁栄→破壊→荒廃）。見た目は Limbo/Inside の層の空気と小さな人体、ジオラマを大判で撮る距離感。

## 禁止
絵本調・ドット・ローポリゲーム美・フォトリアル都市・PBR鏡面 / 家の擬人化・顔アップ・強制カットシーン・主題の説明文 /
年代ラベル（西暦・「氷河期」等）を画面に出す / 戦争の爆発デモ・個別事件の再現 / GLB大量読込・建物の手モデリング /
デスクトップ前提UI・ホバー必須・ページスクロールとカメラの競合 / 起動タイトル・ゲームオーバー・スタッフロール・実績 /
メロディの目立つBGM・ループ感・著作権音源 / 未使用フレームワーク・過剰な抽象化。

## 技術（固定）
Vite + TypeScript + Three.js(WebGL2)。React/R3F・物理・WebGPUなし。地形＝ハイトマップ＋頂点シェーダ変位。
建物＝原型メッシュのInstancedMesh。植生＝インスタンス＋遠景ポイント。人物＝顔なしの塊。音＝Web Audio のみ。
ポスト＝DOF必須、薄いグレイン、軽い色収差。DPR上限2、重いと1.25まで自動低下。ドローコール通常80以下・最悪150。

## 操作（変更しない）
1本指=オービット(慣性) / ピンチ=距離(対数, 1.2m〜9km) / 2本指ドラッグ=注視点移動(家の座標へスプリング) /
下部スクラブ=時間(数字なし、目盛りは粗密のみ) / 速度=停止・遅・中・速 / ミュート常時。canvas は touch-action:none。
2本指は最初の約10pxでピンチかパンに確定し、指を離すまで混ぜない。

## 時間
内部値 u∈[0,1] → 年（設計キー、表示しない）。`src/time/time-model.ts`。
1900–1920 春の丘 / –1950 砂利・隣家・畑 / –1990 舗装・電柱・商店 / –2050 密集・中層 / –2150 高層が家を覆い家が消える /
–2300 巨大化・光害 / 混乱(短) / 衰退・植物侵入 / 無人(千年単位) / 深時間(対数: 氷・海進・侵食)。
人間区間は季節が回る（高速スクラブ中は季節を中立へ寄せる）。深時間は季節を捨てる。昼夜は壁時計（1日=240秒）、終端で黄昏に固定。

## 原点（家の礎）
座標は最後まで内部に残す（`TouchCamera.anchor`）。痕跡の順序: 家 → 基礎石・井戸・リンゴの木・段差 →
都市の中の正方形の空き地（夜は光が無い） → 石列と不自然にまっすぐな微高地 → 何もない。大切そうに演出しない。

## 音
最初のタップで AudioContext を解錠。バス: ambience / drone / spot / ui → 高度EQ（寄ると高域・細部、引くと風と低域）→ 穴（沈黙）→ master。
ステムは t でクロスフェード。イベント系は毎回生成し同じフレーズを繰り返さない。スクラブ速度で引き伸ばし/粒立ち。
音源ファイルは未配置。全て手続き音で、各クラスに「replace with: public/audio/…」の差し替え位置コメントあり。

## 終端
END_DESOLATION(低高度: 同じ輪郭の丘・黄昏・風) / END_SEA(高高度: 海岸線が原点を越える) / END_QUIET(無操作でUIが消える)。
同時成立可。選択UIなし。数秒保つと中央やや下に作品名を小さくフェードイン（Phase 4）。スクラブを戻せば即消える。

## フェーズ
0 丘・家・人・DOF・風 → 1 道の変化・隣家・電柱・リンゴ → 2 高層都市・家の消失・夜と光害・灯と点（v1公開点）→
3 欠損・植物・崩壊・混乱 → 4 対数時間・地形・海進・終端。飛ばさない。各フェーズ末に README「今何ができるか」を更新。

## コード
短いファイル。神クラス禁止。コメントは「なぜ」だけ。物語をコメントに書かない。シードは定数（`WORLD_SEED`）。
全ワールドマテリアルは `render/shared.ts` の共有uniformと `render/glsl.ts` の共通GLSLを使う（塗った塊の照明・影・霧）。
影を落とすメッシュは `SunShadow.add(mesh, depthMaterial)` に登録（layer 1）。

## 検証
`npm run dev` → `?debug` で fps/ドローコール/内部年を表示（通常は非表示）。`?t=年&d=距離&az=&el=&day=&nointro` で直接その状態へ。
